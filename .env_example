# Environment variables for WeatherAgent
# Copy this file to .env (already named) and fill in required values.
# NOTE: Do NOT commit real secrets to source control.

# === Required (no defaults in code) ===
# Azure AI Project endpoint (e.g. https://YOUR-RESOURCE-NAME.services.ai.azure.com/api/projects/YOUR-PROJECT-NAME)
PROJECT_ENDPOINT=

BASE_URL=https://YOUR-RESOURCE-NAME.services.ai.azure.com
# Name (deployment name) of the Azure OpenAI / model deployment to use
MODEL_DEPLOYMENT_NAME=gpt-4o

# === Optional (defaults provided in code) ===
# MCP server base URL (default in code if unset)
MCP_SERVER_URL=https://YOUR-MCP-SERVER-NAME/mcp
# Label used for the MCP server (default in code if unset)
MCP_SERVER_LABEL=weathermcpserver

AGENT_ID=YOUR-AGENT-ID-FROM-FOUNDRY

CONNECTED_AGENT_ID=YOUR-AGENT-ID-FROM-FOUNDRY

# === Authentication for DefaultAzureCredential (choose ONE method) ===
# DefaultAzureCredential will try multiple sources (Azure CLI login, Managed Identity, Environment, etc.).
# If you prefer environment-based auth, uncomment and fill the following:
# AZURE_TENANT_ID=
# AZURE_CLIENT_ID=
# AZURE_CLIENT_SECRET=

# If using a user account via Azure CLI, you can login instead of setting the above:
#   az login

# === Optional: HTTP(s) proxy settings (if behind corporate proxy) ===
# HTTPS_PROXY=
# HTTP_PROXY=

# === Tool-specific dynamic headers (set only if your MCP tool/server requires them) ===
# SUPER_SECRET_HEADER_KEY=SuperSecret
# SUPER_SECRET_HEADER_VALUE=123456

# === Monitoring ===
# Connection string for Application Insights (Azure Monitor). Required for telemetry export.
APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=YOUR-APPINSIGHT-CONNECTION-STRING

# After editing, load these vars (if using python-dotenv) before running the script.
# Example (optional):
#   pip install python-dotenv
#   from dotenv import load_dotenv; load_dotenv()
FILES_VECTOR_STORE_IDS=YOUR-FILE-STORE-IN-FOUNDRY